const DEBUG=false;const log={info:(...args)=>DEBUG&&console.log('[INFO]',...args),error:(...args)=>console.error('[ERROR]',...args),warn:(...args)=>DEBUG&&console.warn('[WARN]',...args)};const sanitize={html:(str)=>{if(typeof str!=='string')return '';const div=document.createElement('div');div.textContent=str;return div.innerHTML},number:(val)=>{const num=parseFloat(val);return isNaN(num)?0:num},path:(path)=>{if(typeof path!=='string')return '';if(path.includes('..')||path.includes('~')){log.warn('Potentially unsafe path detected:',path);return ''}return path}};const retry=async(fn,maxAttempts=3,delay=1000)=>{for(let attempt=1;attempt<=maxAttempts;attempt++){try{return await fn()}catch(error){if(attempt===maxAttempts)throw error;const waitTime=delay*attempt;log.warn(`Attempt ${attempt} failed, retrying in ${waitTime}ms...`);announce(`Retrying operation (attempt ${attempt+1} of ${maxAttempts})`,'polite');await new Promise(resolve=>setTimeout(resolve,waitTime))}}};const announce=(message,priority='polite')=>{const announcer=document.getElementById('announcements');if(!announcer)return;announcer.setAttribute('aria-live',priority);announcer.textContent=message;setTimeout(()=>{announcer.textContent=''},1000)};let selectedImagePath=null,extractionResults=null,sessionStats={total:0,successful:0,totalCoverage:0,coverageCount:0};const selectImageBtn=document.getElementById('selectImageBtn'),extractBtn=document.getElementById('extractBtn'),saveBtn=document.getElementById('saveBtn'),toggleJsonBtn=document.getElementById('toggleJson'),removeImageBtn=document.getElementById('removeImage'),uploadArea=document.getElementById('uploadArea'),imagePreview=document.getElementById('imagePreview'),previewImg=document.getElementById('previewImg'),imagePath=document.getElementById('imagePath'),fileSize=document.getElementById('fileSize'),engineOCR=document.getElementById('engineOCR'),engineFlorence2=document.getElementById('engineFlorence2'),engineSROIE=document.getElementById('engineSROIE'),engineHybrid=document.getElementById('engineHybrid'),aiSettings=document.getElementById('aiSettings'),aiMode=document.getElementById('aiMode'),progressSection=document.getElementById('progressSection'),progressLog=document.getElementById('progressLog'),resultsDisplay=document.getElementById('resultsDisplay'),emptyState=document.getElementById('emptyState');document.addEventListener('DOMContentLoaded',()=>{log.info('App initialized');if(!window.electronAPI){log.error('electronAPI not available!');showError('Application Error','Unable to connect to application backend. Please restart the app.');return}loadSettings();setupEventListeners();updateSessionStats();checkDependencies();log.info('All systems ready')});function setupEventListeners(){function updateEngineSelection(){if(engineOCR.checked){aiSettings.style.display='none';updateEngineInfo('ocr')}else if(engineFlorence2.checked){aiSettings.style.display='block';updateEngineInfo('florence2')}else if(engineSROIE.checked){aiSettings.style.display='block';updateEngineInfo('donut_cord')}else if(engineHybrid.checked){aiSettings.style.display='block';updateEngineInfo('hybrid')}saveSettings()}engineOCR.addEventListener('change',updateEngineSelection);engineFlorence2.addEventListener('change',updateEngineSelection);engineSROIE.addEventListener('change',updateEngineSelection);engineHybrid.addEventListener('change',updateEngineSelection);const modeButtons=document.querySelectorAll('.mode-btn');modeButtons.forEach(btn=>{btn.addEventListener('click',()=>{modeButtons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');aiMode.value=btn.dataset.mode;saveSettings()})});selectImageBtn.addEventListener('click',handleSelectImage);if(uploadArea){uploadArea.addEventListener('click',handleSelectImage);uploadArea.style.cursor='pointer';uploadArea.addEventListener('dragover',handleDragOver);uploadArea.addEventListener('dragleave',handleDragLeave);uploadArea.addEventListener('drop',handleDrop)}if(removeImageBtn){removeImageBtn.addEventListener('click',handleRemoveImage)}extractBtn.addEventListener('click',handleExtract);saveBtn.addEventListener('click',handleSave);toggleJsonBtn.addEventListener('click',handleToggleJson);window.electronAPI.onProgress((data)=>{if(progressLog){const shouldScroll=progressLog.scrollTop+progressLog.clientHeight>=progressLog.scrollHeight-10;progressLog.textContent+=data+'\n';if(shouldScroll){progressLog.scrollTop=progressLog.scrollHeight}}});const keyboardShortcuts={'Control+o':()=>handleSelectImage(),'Meta+o':()=>handleSelectImage(),'Control+e':()=>!extractBtn.disabled&&handleExtract(),'Meta+e':()=>!extractBtn.disabled&&handleExtract(),'Control+s':()=>!saveBtn.disabled&&handleSave(),'Meta+s':()=>!saveBtn.disabled&&handleSave(),'Control+j':()=>handleToggleJson(),'Meta+j':()=>handleToggleJson(),'Delete':()=>selectedImagePath&&handleRemoveImage(),'1':()=>selectEngine('ocr'),'2':()=>selectEngine('florence2'),'3':()=>selectEngine('donut_cord'),'4':()=>selectEngine('hybrid')};document.addEventListener('keydown',(e)=>{if(e.target.tagName==='INPUT'||e.target.tagName==='SELECT'||e.target.tagName==='TEXTAREA'){return}const key=`${e.ctrlKey?'Control+':''}${e.metaKey?'Meta+':''}${e.key.toLowerCase()}`;const handler=keyboardShortcuts[key]||keyboardShortcuts[e.key];if(handler){e.preventDefault();handler()}});announce('Receipt Extractor ready. Press Ctrl+O to select an image.','polite')}function selectEngine(engineName){const engines={'ocr':engineOCR,'ocr_easyocr':engineOCR,'florence2':engineFlorence2,'donut_cord':engineSROIE,'hybrid':engineHybrid};const engine=engines[engineName];if(engine){engine.checked=true;engine.dispatchEvent(new Event('change'));announce(`Selected ${engineName} engine`,'polite')}}async function handleSelectImage(){log.info('Select image clicked');try{log.info('Calling electronAPI.selectImage()...');const result=await retry(async()=>await window.electronAPI.selectImage());log.info('Got result:',result);if(result&&result.success&&result.path){const validPath=sanitize.path(result.path);if(!validPath){throw new Error('Invalid file path')}selectedImagePath=validPath;log.info('Image selected:',selectedImagePath);imagePreview.classList.remove('hidden');previewImg.src=`file://${sanitize.path(result.path)}`;const fileName=sanitize.html(result.path.split(/[\\\/]/).pop());imagePath.textContent=fileName;try{const stats=await window.electronAPI.getFileStats(validPath);if(stats&&stats.size){const sizeMB=(stats.size/(1024*1024)).toFixed(2);fileSize.textContent=`${sizeMB} MB`}}catch(e){fileSize.textContent=''}extractBtn.disabled=false;document.getElementById('extract-status').textContent='Ready to extract';announce(`Image selected: ${fileName}`,'polite');log.info('Image preview updated')}else{log.info('Selection cancelled or failed')}}catch(error){log.error('Error selecting image:',error);showError('Selection Error',sanitize.html(error.message));announce('Error selecting image','assertive')}}function handleRemoveImage(){selectedImagePath=null;imagePreview.classList.add('hidden');extractBtn.disabled=true;document.getElementById('extract-status').textContent='Select an image first to enable extraction';previewImg.src='';imagePath.textContent='';fileSize.textContent='';announce('Image removed','polite')}function handleDragOver(e){e.preventDefault();e.stopPropagation();if(uploadArea){uploadArea.style.borderColor='var(--color-primary)';uploadArea.style.backgroundColor='rgba(0, 176, 240, 0.05)'}}function handleDragLeave(e){e.preventDefault();e.stopPropagation();if(uploadArea){uploadArea.style.borderColor='';uploadArea.style.backgroundColor=''}}async function handleDrop(e){e.preventDefault();e.stopPropagation();if(uploadArea){uploadArea.style.borderColor='';uploadArea.style.backgroundColor=''}const files=e.dataTransfer.files;if(files.length===0)return;const file=files[0],filePath=file.path;const validExtensions=['.jpg','.jpeg','.png','.bmp','.tiff','.tif'],ext=filePath.toLowerCase().substring(filePath.lastIndexOf('.'));if(!validExtensions.includes(ext)){showError('Invalid File Type','Please drop a valid image file (JPG, PNG, BMP, TIFF)');announce('Invalid file type','assertive');return}try{const validPath=sanitize.path(filePath);if(!validPath){throw new Error('Invalid file path')}selectedImagePath=validPath;log.info('Image dropped:',selectedImagePath);imagePreview.classList.remove('hidden');previewImg.src=`file://${sanitize.path(filePath)}`;const fileName=sanitize.html(filePath.split(/[\\\/]/).pop());imagePath.textContent=fileName;try{const stats=await window.electronAPI.getFileStats(validPath);if(stats&&stats.size){const sizeMB=(stats.size/(1024*1024)).toFixed(2);fileSize.textContent=`${sizeMB} MB`}}catch(e){fileSize.textContent=''}extractBtn.disabled=false;announce(`Image dropped: ${fileName}`,'polite');log.info('Image preview updated via drag & drop')}catch(error){log.error('Error handling dropped file:',error);showError('Drop Error',sanitize.html(error.message));announce('Error handling dropped file','assertive')}}async function handleExtract(){if(!selectedImagePath){log.warn('No image selected');return}let selectedEngine='ocr_easyocr';if(engineFlorence2.checked)selectedEngine='florence2';else if(engineSROIE.checked)selectedEngine='donut_cord';else if(engineHybrid.checked)selectedEngine='hybrid';log.info('Starting extraction with:',selectedEngine);extractBtn.disabled=true;extractBtn.textContent='Processing...';progressLog.textContent='';progressSection.classList.remove('hidden');resultsDisplay.classList.add('hidden');emptyState.classList.add('hidden');const engineBadgeText=document.getElementById('engineBadgeText');if(engineBadgeText){const badgeNames={'ocr':'OCR','ocr_easyocr':'OCR','ocr_tesseract':'Tesseract','ocr_paddle':'PaddleOCR','florence2':'Florence-2','donut_cord':'Donut CORD','hybrid':'Hybrid'};engineBadgeText.textContent=badgeNames[selectedEngine]||'OCR'}announce(`Starting extraction with ${selectedEngine} engine`,'polite');const startTime=Date.now();try{const extractionOptions={imagePath:selectedImagePath,model:selectedEngine,mode:selectedEngine!=='ocr'?aiMode.value:'fast'};log.info('Extraction options:',extractionOptions);const result=await retry(async()=>await window.electronAPI.extractReceipt(extractionOptions),3,2000);const duration=((Date.now()-startTime)/1000).toFixed(1);log.info('Extraction completed in',duration,'s');log.info('Result:',result);if(result&&result.success){extractionResults=result.data;sessionStats.total++;sessionStats.successful++;const receipt=result.data.receipt||result.data.best_result?.receipt||result.data,coverage=receipt.coverage||receipt.extraction_coverage;if(coverage&&coverage!=='N/A'){const coverageNum=parseFloat(coverage);sessionStats.totalCoverage+=coverageNum;sessionStats.coverageCount++}const itemCount=receipt.items?.length||0;updateSessionStats();progressSection.classList.add('hidden');displayResults(result.data,duration,selectedEngine);saveBtn.disabled=false;announce(`Extraction completed successfully in ${duration} seconds. ${itemCount} items extracted.`,'polite')}else{sessionStats.total++;updateSessionStats();log.error('Extraction failed:',result?.error);const errorMsg=sanitize.html(result?.error||'Extraction Failed'),errorDetails=sanitize.html(result?.details||'Unknown error occurred');showError(errorMsg,errorDetails);announce('Extraction failed','assertive')}}catch(error){sessionStats.total++;updateSessionStats();log.error('Extraction exception:',error);showError('Extraction Failed',sanitize.html(error.message));announce('Extraction failed after multiple attempts','assertive')}finally{extractBtn.disabled=false;extractBtn.textContent='Extract Receipt'}}function syntaxHighlight(json){if(typeof json!=='string'){json=JSON.stringify(json,null,2)}json=json.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');return json.replace(/("(\\u[a-zA-Z0-9]{4}|\\[^u]|[^\\"])*"(\s*:)?|\b(true|false|null)\b|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?)/g,function(match){let cls='json-number';if(/^"/.test(match)){if(/:$/.test(match)){cls='json-key'}else{cls='json-string'}}else if(/true|false/.test(match)){cls='json-boolean'}else if(/null/.test(match)){cls='json-null'}return '<span class="'+cls+'">'+match+'</span>'})}function displayResults(data,duration,engine){log.info('Displaying results');emptyState.classList.add('hidden');resultsDisplay.classList.remove('hidden');const resultEngineEl=document.getElementById('resultEngine'),resultEngineText=document.getElementById('resultEngineText');if(resultEngineText){resultEngineText.textContent=engine==='ocr'?'OCR':'Donut AI'}let receipt,isQualityMode=false;if(data.best_result&&data.all_results){isQualityMode=true;receipt=data.best_result.receipt;displayStrategyComparison(data.all_results)}else if(data.receipt){receipt=data.receipt}else{receipt=data}const storeName=receipt.store?.name||'Not detected',itemCount=receipt.item_count||receipt.items?.length||0,total=Number(receipt.totals?.total||receipt.total||0).toFixed(2),coverage=receipt.coverage||receipt.extraction_coverage||'N/A';document.getElementById('storeName').textContent=storeName;document.getElementById('itemCount').textContent=itemCount;document.getElementById('totalAmount').textContent=`$${total}`;document.getElementById('coverage').textContent=coverage;displayItems(receipt.items||[]);displayNotes(receipt.notes||receipt.extraction_notes||[]);displayValidation(receipt,duration);document.getElementById('jsonDisplay').innerHTML=syntaxHighlight(data);log.info('Results displayed')}function displayItems(items){const tbody=document.getElementById('itemsTableBody');tbody.innerHTML='';if(!items||items.length===0){const row=document.createElement('tr');row.setAttribute('role','row');const cell=document.createElement('td');cell.setAttribute('role','cell');cell.setAttribute('colspan','4');cell.className='text-center text-muted';cell.textContent='No items extracted';row.appendChild(cell);tbody.appendChild(row);return}items.forEach(item=>{const row=document.createElement('tr');row.setAttribute('role','row');const nameCell=document.createElement('td');nameCell.setAttribute('role','cell');nameCell.textContent=item.name||'-';row.appendChild(nameCell);const qtyCell=document.createElement('td');qtyCell.setAttribute('role','cell');qtyCell.className='text-center';qtyCell.textContent=Number(item.quantity)||1;row.appendChild(qtyCell);const priceCell=document.createElement('td');priceCell.setAttribute('role','cell');priceCell.className='text-right';const unitPrice=item.unit_price?Number(item.unit_price).toFixed(2):'-';priceCell.textContent=unitPrice==='-'?unitPrice:`$${unitPrice}`;row.appendChild(priceCell);const totalCell=document.createElement('td');totalCell.setAttribute('role','cell');totalCell.className='text-right';const totalPrice=Number(item.total_price||0).toFixed(2);totalCell.textContent=`$${totalPrice}`;row.appendChild(totalCell);tbody.appendChild(row)})}function displayNotes(notes){const notesSection=document.getElementById('extractionNotes'),notesList=document.getElementById('notesList');if(!notes||notes.length===0){notesSection.classList.add('hidden');return}notesList.innerHTML='';notes.forEach(note=>{const li=document.createElement('li');li.textContent=note;notesList.appendChild(li)});notesSection.classList.remove('hidden')}function displayValidation(receipt,duration){const validationSection=document.getElementById('validationStatus'),validationList=document.getElementById('validationList');validationList.innerHTML='';const checks=[];if(receipt.store?.name){checks.push({passed:true,text:`Store: ${receipt.store.name}`})}else{checks.push({passed:false,text:'Store not detected'})}const itemCount=receipt.items?.length||0;if(itemCount>0){checks.push({passed:true,text:`${itemCount} items found`})}else{checks.push({passed:false,text:'No items found'})}if(receipt.totals?.total||receipt.total){checks.push({passed:true,text:`Total: $${receipt.totals?.total||receipt.total}`})}else{checks.push({passed:false,text:'Total not found'})}checks.push({passed:true,text:`Completed in ${duration}s`});checks.forEach(check=>{const li=document.createElement('li');li.textContent=`${check.passed?'✓':'✗'} ${check.text}`;li.style.color=check.passed?'var(--color-success)':'var(--color-error)';validationList.appendChild(li)});validationSection.classList.remove('hidden')}function displayStrategyComparison(allResults){const comparisonSection=document.getElementById('strategyComparison'),cards=document.getElementById('strategyCards');if(!allResults||allResults.length===0){comparisonSection.classList.add('hidden');return}cards.innerHTML='';comparisonSection.classList.remove('hidden');let bestScore=0,bestIndex=0;allResults.forEach((result,index)=>{if(result.success&&result.item_count>bestScore){bestScore=result.item_count;bestIndex=index}});allResults.forEach((result,index)=>{const card=document.createElement('div');card.className='strategy-card';if(index===bestIndex&&result.success){card.classList.add('best')}if(result.success){const receipt=result.receipt,coverage=receipt.coverage||receipt.extraction_coverage||'N/A',confidence=receipt.confidence||result.confidence||'N/A';card.innerHTML=`<h4>${result.strategy}${index===bestIndex?'<span class="badge">Best</span>':''}</h4><p><strong>${result.item_count}</strong> items</p><p>Coverage: ${coverage}</p><p>Confidence: ${confidence}</p>`}else{card.innerHTML=`<h4>${result.strategy}</h4><p style="color: var(--color-error);">Failed</p><p style="font-size: 12px;">${result.error||'Unknown error'}</p>`}cards.appendChild(card)})}function showError(title,details){log.error('Showing error:',title,details);resultsDisplay.classList.add('hidden');emptyState.classList.remove('hidden');progressSection.classList.add('hidden');const h3=emptyState.querySelector('h3'),p=emptyState.querySelector('p');if(h3)h3.textContent=title;if(p)p.textContent=details||'Please try again'}async function handleSave(){if(!extractionResults)return;saveBtn.disabled=true;const originalText=saveBtn.textContent;saveBtn.textContent='Saving...';try{const fileName=selectedImagePath.split(/[\\\/]/).pop().replace(/\.[^/.]+$/,''),engine=engineOCR.checked?'ocr':'donut';const result=await window.electronAPI.saveResults({data:extractionResults,defaultPath:`${fileName}_${engine}_extraction.json`});if(result&&result.success){saveBtn.textContent='✓ Saved';announce('Results saved successfully','polite');setTimeout(()=>{saveBtn.textContent=originalText;saveBtn.disabled=false},2000)}else{saveBtn.textContent=originalText;saveBtn.disabled=false;showError('Save Failed','Could not save file');announce('Save failed','assertive')}}catch(error){log.error('Save error:',error);saveBtn.textContent=originalText;saveBtn.disabled=false;showError('Save Failed',sanitize.html(error.message));announce('Save failed','assertive')}}function handleToggleJson(){const jsonSection=document.getElementById('jsonSection');if(!jsonSection)return;const isHidden=jsonSection.classList.contains('hidden');if(isHidden){jsonSection.classList.remove('hidden');toggleJsonBtn.textContent='Hide JSON';toggleJsonBtn.setAttribute('aria-pressed','true');announce('JSON view shown','polite')}else{jsonSection.classList.add('hidden');toggleJsonBtn.textContent='JSON';toggleJsonBtn.setAttribute('aria-pressed','false');announce('JSON view hidden','polite')}}function updateEngineInfo(engine){const infoCard=document.querySelector('.info-card');if(!infoCard)return;const infoContent=infoCard.querySelector('.info-content');if(!infoContent)return;const engineInfo={'ocr':{title:'OCR Method',text:'Fast and reliable for clear, printed receipts. Uses EasyOCR engine. Processing time: 1-2 seconds.'},'ocr_easyocr':{title:'EasyOCR',text:'Fast and reliable for clear, printed receipts. Supports 80+ languages. Processing time: 1-2 seconds.'},'florence2':{title:'Florence-2 (RECOMMENDED)',text:'Microsoft Florence-2 with fast OCR and region detection. Best public model with no authentication required. Processing time: 2-5 seconds.'},'donut_cord':{title:'Donut CORD Model',text:'AI-powered extraction with structured parsing. Good for various receipt formats. Processing time: 5-10 seconds.'},'hybrid':{title:'Hybrid Multi-Model',text:'Combines multiple models for maximum accuracy. Uses best results from each model. Processing time: 10-20 seconds.'}};const info=engineInfo[engine]||engineInfo.ocr;infoContent.innerHTML=`<p class="info-title">${info.title}</p><p class="info-text">${info.text}</p>`}function updateSessionStats(){const sessionBadge=document.getElementById('sessionBadge'),sessionCount=document.getElementById('sessionCount');if(sessionStats.total>0){if(sessionBadge)sessionBadge.style.display='flex';if(sessionCount)sessionCount.textContent=sessionStats.total}}async function loadSettings(){try{const settings=await window.electronAPI.getSettings();log.info('Loaded settings:',settings);const model=settings.lastModel||'ocr_easyocr';engineOCR.checked=false;engineFlorence2.checked=false;engineSROIE.checked=false;engineHybrid.checked=false;if(model==='florence2')engineFlorence2.checked=true;else if(model==='donut_cord'||model==='cord'||model==='sroie')engineSROIE.checked=true;else if(model==='hybrid'||model==='adamcodd')engineHybrid.checked=true;else engineOCR.checked=true;if(model==='ocr'||model==='ocr_easyocr'||model==='ocr_tesseract'||model==='ocr_paddle'){aiSettings.style.display='none'}else{aiSettings.style.display='block'}if(settings.aiMode){aiMode.value=settings.aiMode;const modeButtons=document.querySelectorAll('.mode-btn');modeButtons.forEach(btn=>{btn.classList.toggle('active',btn.dataset.mode===settings.aiMode)})}updateEngineInfo(model)}catch(error){log.error('Error loading settings:',error)}}function saveSettings(){let selectedModel='ocr_easyocr';if(engineFlorence2.checked)selectedModel='florence2';else if(engineSROIE.checked)selectedModel='donut_cord';else if(engineHybrid.checked)selectedModel='hybrid';const settings={lastModel:selectedModel,aiMode:aiMode.value};log.info('Saving settings:',settings);window.electronAPI.saveSettings(settings)}async function checkDependencies(){try{const result=await window.electronAPI.checkDependencies();if(!result.python||!result.dependencies){log.warn('Dependencies missing:',result.message);const sidebar=document.querySelector('.sidebar');if(sidebar){const warning=document.createElement('div');warning.style.cssText=`padding: var(--space-4); background: #fef3c7; border: 1px solid #fbbf24; border-radius: var(--radius-md); margin-bottom: var(--space-4);`;warning.innerHTML=`<p style="font-size: 13px; font-weight: 600; color: #92400e; margin-bottom: 4px;">âš ï¸ Setup Required</p><p style="font-size: 12px; color: #92400e;">${result.message}</p>`;sidebar.insertBefore(warning,sidebar.firstChild)}extractBtn.disabled=true;extractBtn.textContent='Dependencies Missing'}else{log.info('All dependencies ready')}}catch(error){log.error('Error checking dependencies:',error)}}
