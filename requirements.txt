# Core dependencies for Receipt Extractor
# Install with: pip install -r requirements.txt

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# OCR engines (choose at least one)
pytesseract>=0.3.10      # Tesseract OCR (requires tesseract binary installed)
easyocr>=1.7.0           # EasyOCR - RECOMMENDED (no external deps needed)
paddleocr>=2.7.0         # PaddleOCR (optional, enterprise-grade)
paddlepaddle>=2.5.0      # Required for PaddleOCR

# Optional: Advanced AI models
# Uncomment if you want to use Florence-2 or other transformer models
# torch>=2.0.0
# transformers>=4.30.0
# sentencepiece>=0.1.99

# Web app dependencies (if using web interface)
flask>=2.3.0
flask-cors>=4.0.0
werkzeug>=2.3.0

# Testing
pytest>=7.4.0

# Note: For Tesseract OCR, you also need to install the Tesseract binary:
# - Windows: https://github.com/UB-Mannheim/tesseract/wiki
# - macOS: brew install tesseract
# - Linux: sudo apt-get install tesseract-ocr
