const API_BASE_URL='http://localhost:5000/api';let selectedModel=null,availableModels=[],currentExtractionData=null;const elements={modelSelector:document.getElementById('modelSelector'),modelInfo:document.getElementById('modelInfo'),uploadArea:document.getElementById('uploadArea'),fileInput:document.getElementById('fileInput'),imagePreview:document.getElementById('imagePreview'),previewImg:document.getElementById('previewImg'),extractBtn:document.getElementById('extractBtn'),batchExtractBtn:document.getElementById('batchExtractBtn'),actionButtons:document.getElementById('actionButtons'),resultsSection:document.getElementById('resultsSection'),errorSection:document.getElementById('errorSection'),loadingOverlay:document.getElementById('loadingOverlay')};async function init(){setupEventListeners();await loadModels()}function setupEventListeners(){elements.fileInput.addEventListener('change',handleFileSelect);elements.uploadArea.addEventListener('dragover',handleDragOver);elements.uploadArea.addEventListener('dragleave',handleDragLeave);elements.uploadArea.addEventListener('drop',handleDrop);if(elements.extractBtn){elements.extractBtn.addEventListener('click',handleExtract);console.log('Extract button listener attached')}else{console.error('Extract button not found!')}if(elements.batchExtractBtn){elements.batchExtractBtn.addEventListener('click',handleBatchExtract);console.log('Batch extract button listener attached')}else{console.error('Batch extract button not found!')}document.getElementById('exportJson').addEventListener('click',()=>exportData('json'));document.getElementById('exportCsv').addEventListener('click',()=>exportData('csv'));document.getElementById('exportTxt').addEventListener('click',()=>exportData('txt'))}async function loadModels(){try{const response=await fetch(`${API_BASE_URL}/models`),data=await response.json();if(data.success){availableModels=data.models;selectedModel=data.current_model||data.default_model;renderModels()}else{showError('Failed to load models')}}catch(error){console.error('Error loading models:',error);showError('Failed to connect to API server. Make sure the backend is running.')}}function renderModels(){if(availableModels.length===0){elements.modelSelector.innerHTML='<p class="loading">No models available</p>';return}elements.modelSelector.innerHTML='';availableModels.forEach(model=>{const card=document.createElement('div');card.className=`model-card ${model.id===selectedModel?'selected':''}`;card.onclick=()=>selectModel(model.id);const badgeClass=`badge-${model.type}`;card.innerHTML=`<h4>${model.name}</h4><p>${model.description}</p><span class="model-badge ${badgeClass}">${model.type.toUpperCase()}</span>${model.requires_auth?'<span class="model-badge" style="background: #fee2e2; color: #991b1b;">Auth Required</span>':''}`;elements.modelSelector.appendChild(card)});updateModelInfo()}async function selectModel(modelId){try{const response=await fetch(`${API_BASE_URL}/models/select`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model_id:modelId})}),data=await response.json();if(data.success){selectedModel=modelId;renderModels()}else{showError(`Failed to select model: ${data.error}`)}}catch(error){console.error('Error selecting model:',error);showError('Failed to select model')}}function updateModelInfo(){const model=availableModels.find(m=>m.id===selectedModel);if(model){const capabilities=model.capabilities,capList=Object.entries(capabilities).filter(([_,value])=>value).map(([key])=>key.replace('_',' ')).join(', ');elements.modelInfo.innerHTML=`<strong>Selected Model:</strong> ${model.name}<br><strong>Capabilities:</strong> ${capList||'Basic extraction'}`}}function handleFileSelect(event){const file=event.target.files[0];if(file){processFile(file)}}function handleDragOver(event){event.preventDefault();elements.uploadArea.classList.add('dragover')}function handleDragLeave(event){event.preventDefault();elements.uploadArea.classList.remove('dragover')}function handleDrop(event){event.preventDefault();elements.uploadArea.classList.remove('dragover');const file=event.dataTransfer.files[0];if(file&&file.type.startsWith('image/')){processFile(file)}else{showError('Please drop an image file')}}function processFile(file){const maxSize=16*1024*1024;if(file.size>maxSize){showError('File size exceeds 16MB');return}const reader=new FileReader();reader.onload=(e)=>{elements.previewImg.src=e.target.result;elements.imagePreview.classList.remove('hidden');elements.extractBtn.disabled=false;elements.batchExtractBtn.disabled=false;console.log('Buttons enabled:',{extractBtn:!elements.extractBtn.disabled,batchExtractBtn:!elements.batchExtractBtn.disabled})};reader.readAsDataURL(file);elements.fileInput.file=file}async function handleExtract(){const file=elements.fileInput.files[0];if(!file){showError('Please select an image first');return}if(!selectedModel){showError('Please select a model first');return}showLoading(true);hideError();elements.resultsSection.classList.add('hidden');try{const formData=new FormData();formData.append('image',file);formData.append('model_id',selectedModel);const response=await fetch(`${API_BASE_URL}/extract`,{method:'POST',body:formData}),data=await response.json();if(data.success&&data.data){currentExtractionData=data.data;displayResults(data.data)}else{const errorMsg=data.error||'Extraction failed';showError(typeof errorMsg==='object'?errorMsg.message||'Extraction failed':errorMsg)}}catch(error){console.error('Extraction error:',error);showError('Failed to extract receipt data. Please try again.')}finally{showLoading(false)}}async function handleBatchExtract(){console.log('handleBatchExtract called');const file=elements.fileInput.files[0];if(!file){showError('Please select an image first');return}console.log('Starting batch extraction with file:',file.name);showLoading(true,'Processing with ALL models... This may take a few minutes.');hideError();elements.resultsSection.classList.add('hidden');try{const formData=new FormData();formData.append('image',file);const response=await fetch(`${API_BASE_URL}/extract/batch`,{method:'POST',body:formData}),data=await response.json();if(data.success){downloadBatchResults(data);showSuccess(`Successfully extracted with ${data.models_count} models! Results downloaded as JSON.`)}else{showError(data.error||'Batch extraction failed')}}catch(error){console.error('Batch extraction error:',error);showError('Failed to perform batch extraction. Please try again.')}finally{showLoading(false)}}function downloadBatchResults(data){const timestamp=new Date().toISOString().replace(/[:.]/g,'-').slice(0,-5),filename=`batch_extraction_${timestamp}.json`,content=JSON.stringify(data,null,2),blob=new Blob([content],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url)}function displayResults(data){document.getElementById('storeName').textContent=data.store?.name||'-';document.getElementById('storeAddress').textContent=data.store?.address||'-';document.getElementById('storePhone').textContent=data.store?.phone||'-';document.getElementById('transDate').textContent=data.date||'-';document.getElementById('transTime').textContent=data.time||'-';document.getElementById('total').textContent=data.totals?.total?`$${data.totals.total}`:'-';const processingInfo=document.getElementById('processingInfo');processingInfo.innerHTML=`<strong>Model:</strong> ${data.model} | <strong>Processing Time:</strong> ${data.processing_time?.toFixed(2)}s | <strong>Confidence:</strong> ${data.confidence||'N/A'}`;const lineItemsContainer=document.getElementById('lineItems'),itemCount=document.getElementById('itemCount');if(data.items&&data.items.length>0){lineItemsContainer.innerHTML='';data.items.forEach(item=>{const itemDiv=document.createElement('div');itemDiv.className='line-item';itemDiv.innerHTML=`<span class="item-name">${item.name}</span><span class="item-price">$${item.total_price}</span>`;lineItemsContainer.appendChild(itemDiv)});itemCount.textContent=data.items.length}else{lineItemsContainer.innerHTML='<p class="no-items">No items extracted</p>';itemCount.textContent='0'}elements.resultsSection.classList.remove('hidden')}function exportData(format){if(!currentExtractionData){showError('No data to export');return}let content,filename,mimeType;switch(format){case'json':content=JSON.stringify(currentExtractionData,null,2);filename='receipt_data.json';mimeType='application/json';break;case'csv':content=convertToCSV(currentExtractionData);filename='receipt_data.csv';mimeType='text/csv';break;case'txt':content=convertToText(currentExtractionData);filename='receipt_data.txt';mimeType='text/plain';break;default:return}const blob=new Blob([content],{type:mimeType}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url)}function convertToCSV(data){let csv='Item,Quantity,Price\n';if(data.items&&data.items.length>0){data.items.forEach(item=>{csv+=`"${item.name}",${item.quantity},${item.total_price}\n`})}csv+='\nStore Information\n';csv+=`Name,${data.store?.name||''}\n`;csv+=`Address,${data.store?.address||''}\n`;csv+=`Phone,${data.store?.phone||''}\n`;csv+='\nTransaction\n';csv+=`Date,${data.date||''}\n`;csv+=`Total,${data.totals?.total||''}\n`;return csv}function convertToText(data){let text='=== RECEIPT EXTRACTION ===\n\n';text+='--- Store Information ---\n';text+=`Name: ${data.store?.name||'-'}\n`;text+=`Address: ${data.store?.address||'-'}\n`;text+=`Phone: ${data.store?.phone||'-'}\n\n`;text+='--- Transaction Details ---\n';text+=`Date: ${data.date||'-'}\n`;text+=`Total: $${data.totals?.total||'-'}\n\n`;if(data.items&&data.items.length>0){text+='--- Line Items ---\n';data.items.forEach((item,index)=>{text+=`${index+1}. ${item.name} - $${item.total_price}\n`})}text+=`\n--- Extraction Info ---\n`;text+=`Model: ${data.model}\n`;text+=`Processing Time: ${data.processing_time?.toFixed(2)}s\n`;return text}function showLoading(show,message='Processing receipt...'){if(show){elements.loadingOverlay.classList.remove('hidden');const loadingText=elements.loadingOverlay.querySelector('p');if(loadingText){loadingText.textContent=message}}else{elements.loadingOverlay.classList.add('hidden')}}function showError(message){elements.errorSection.classList.remove('hidden');document.getElementById('errorMessage').textContent=message}function showSuccess(message){const errorSection=elements.errorSection;errorSection.style.backgroundColor='#d1fae5';errorSection.style.borderColor='#10b981';errorSection.classList.remove('hidden');document.getElementById('errorMessage').textContent='✓ '+message;document.getElementById('errorMessage').style.color='#065f46';setTimeout(()=>{errorSection.style.backgroundColor='';errorSection.style.borderColor='';document.getElementById('errorMessage').style.color='';hideError()},5000)}function hideError(){elements.errorSection.classList.add('hidden')}document.addEventListener('DOMContentLoaded',init);
